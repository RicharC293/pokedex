import 'package:flutter/painting.dart';

const background = Color(0xFF200F50);
const text = Color(0xFFE0E0E0);
const primary = Color(0xFFF7D548);
const secondary = Color(0xFF4829A2);
const card = Color(0xFF5835E4);

const space0 = 0.0;
const space1 = 4.0;
const space2 = 8.0;
const space3 = 12.0;
const space4 = 16.0;
const space5 = 20.0;
const space6 = 24.0;

const padding0 = EdgeInsets.all(space0);
const padding1 = EdgeInsets.all(space1);
const padding2 = EdgeInsets.all(space2);
const padding3 = EdgeInsets.all(space3);
const padding4 = EdgeInsets.all(space4);
const padding5 = EdgeInsets.all(space5);
const padding6 = EdgeInsets.all(space6);

const margin0 = EdgeInsets.all(space0);
const margin1 = EdgeInsets.all(space1);
const margin2 = EdgeInsets.all(space2);
const margin3 = EdgeInsets.all(space3);
const margin4 = EdgeInsets.all(space4);
const margin5 = EdgeInsets.all(space5);
const margin6 = EdgeInsets.all(space6);

const borderRadius0 = BorderRadius.all(Radius.circular(space0));
const borderRadius1 = BorderRadius.all(Radius.circular(space1));
const borderRadius2 = BorderRadius.all(Radius.circular(space2));
const borderRadius3 = BorderRadius.all(Radius.circular(space3));
const borderRadius4 = BorderRadius.all(Radius.circular(space4));
const borderRadius5 = BorderRadius.all(Radius.circular(space5));
const borderRadius6 = BorderRadius.all(Radius.circular(space6));

const baseUri = 'https://pokeapi.co/api/v2';

/// Replace <INDEX> with the index of the pokemon
const baseSvgPicture =
    "https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/dream-world/<INDEX>.svg";
